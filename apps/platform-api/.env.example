# Listen port inside the API process / container (Compose maps host → this port).
PLATFORM_PORT=4100

# Docker Compose sets DB + public URLs. Channel / Shopify / login live in Settings.
JWT_SECRET="dev-jwt-secret-do-not-use-in-prod-v2"
