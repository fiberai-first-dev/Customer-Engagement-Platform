services:
  # cep-postgres:
  #   image: postgres:16
  #   environment:
  #     POSTGRES_USER: cep
  #     POSTGRES_PASSWORD: cep
  #     POSTGRES_DB: cep_platform
  #   ports:
  #     - "5434:5432"
  #   volumes:
  #     - cep_pg_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U cep -d cep_platform"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 10

  # Bootstrap: platform-api runs SQL migrate/repair at boot (no prisma migrate deploy).
  # Fresh DB: `cd platform-api && npm run bootstrap` then `docker compose up -d --build`
  # See platform-api/BOOTSTRAP.md
  platform-api:
    build:
      context: ./platform-api
      dockerfile: Dockerfile
    env_file:
      - ./platform-api/.env
    ports:
      - "4100:4100"
    # depends_on:
    #   cep-postgres:
    #     condition: service_healthy

  platform-web:
    build:
      context: ./platform-web
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://cep-api.logback-backend-services.online}
    env_file:
      - ./platform-web/.env
    ports:
      - "9000:5173"
    # depends_on:
    #   - platform-api

# volumes:
#   cep_pg_data:
